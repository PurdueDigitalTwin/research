name: Auto approve

on:
  pull_request:
    branches: [master]

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: |
      github.actor == 'juanwulu' ||
      github.actor == 'dependabot[bot]'
    steps:
      - uses: hmarr/auto-approve-action@v4
