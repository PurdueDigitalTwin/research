\section{Experiments}
\label{sec: experiment}

We test three claims directly: (i) per-step gradient variance scales with $\boldsymbol{g}^{\!\top}\!\mJ\Sigma_{\vv'}\mJ^{\!\top}\boldsymbol{g}$ as predicted by~\Cref{thm: jacobi-variance}; (ii) approaching the control-variate optimum $\beta\!\to\!1$ via VaMF reduces gradient variance by a measurable factor; (iii) the variance reduction translates to better sample quality. All toy and DGMM experiments use a $3$-layer MLP with $128$ hidden units, $200{,}000$ optimization steps, and three random seeds $\{42,0,1\}$. Code, configurations, and result JSONs are released with the paper.

\subsection{Direct Measurement of Gradient Variance}
\label{subsec: exp-grad-var}

\Cref{fig: nr-vs-beta} reports the mean per-step gradient covariance trace $\Tr(\Cov[\boldsymbol{g}])$ as a function of the tangent coefficient $\beta\!\in\!\{0,0.25,0.5,0.75,1\}$ on four 2-D toy datasets (eight Gaussians, two moons, swiss roll, pinwheel). At each measurement step, we draw $K\!=\!8$ independent mini-batches and compute the sample variance of their gradients across parameters. The variance is monotonically decreasing in $\beta$ on every dataset, and the reduction at $\beta\!=\!1$ relative to vanilla MeanFlow ($\beta\!=\!0$) is $4.7\!\times$ on eight Gaussians, $3.8\!\times$ on two moons, $3.2\!\times$ on pinwheel, and $1.7\!\times$ on swiss roll. This directly verifies~\Cref{thm: jacobi-variance}: replacing the conditional fluctuation $\vv'$ in the JVP tangent with a deterministic proxy eliminates the dominant Jacobian-amplified contribution to gradient variance. The right panel further shows that the reduction holds across the full training trajectory, not just at convergence, confirming that the amplification is operative throughout training.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{assets/figures/nr_vs_beta.pdf}
    \caption{Direct gradient-variance measurement during training (\Cref{thm: jacobi-variance} validation). \textbf{(a)} Mean $\Tr(\Cov[\boldsymbol{g}])$ over the second half of training, per dataset, as a function of the tangent coefficient $\beta$. The variance drops monotonically with $\beta$, with $3.2\!\times\!\!\sim\!4.7\!\times$ reduction at $\beta\!=\!1$ on three of four datasets. \textbf{(b)} Variance trajectory across training on \texttt{swiss\_roll} (solid) and \texttt{pinwheel} (dashed), confirming the reduction is operative throughout training, not just at convergence.}
    \label{fig: nr-vs-beta}
\end{figure}

\subsection{Toy Benchmark}
\label{subsec: exp-toy}

\Cref{tab: toy} reports SW$_1$ and SW$_2$ on six 2-D toy datasets averaged over three random seeds. VaMF (with EMA proxy + FM anchor, corresponding to $\beta\!\to\!1$ in our framework) outperforms vanilla MeanFlow on five of six datasets, with relative SW$_1$ improvements of $-23.7\%$ (\texttt{checkerboard}), $-13.2\%$ (\texttt{eight\_gaussians}), $-22.5\%$ (\texttt{two\_moons}), $-53.7\%$ (\texttt{swiss\_roll}), and $-37.2\%$ (\texttt{pinwheel}). On \texttt{two\_spirals}, where MeanFlow already achieves the lowest absolute SW$_1$ ($0.034$) of any dataset, VaMF is marginally worse ($+7.9\%$ on a small absolute base). The SW$_2$ improvements broadly follow the SW$_1$ pattern, with even larger margins on \texttt{swiss\_roll} ($-66.0\%$) and \texttt{two\_moons} ($-47.6\%$) where mode-mixing creates heavy tails in the residual distribution.~\Cref{fig: toy-samples} visualizes samples: VaMF recovers structural details---spirals, sharp corners, mode separation---that vanilla MeanFlow blurs out.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{assets/figures/toy_samples.pdf}
    \caption{Reference distribution and final-step samples on the 2-D toy datasets at seed $42$. VaMF (bottom row) recovers structural details---spirals, sharp corners, mode separation---that vanilla MeanFlow blurs out, especially on \texttt{checkerboard} and \texttt{swiss\_roll}.}
    \label{fig: toy-samples}
\end{figure}

\begin{table}[t]
    \centering
    \caption{Sliced Wasserstein distance (lower is better) on the 2-D toy benchmark, averaged over three seeds $\{42,0,1\}$ at $200$k steps. SW$_p$ is evaluated on $4096$ samples with $500$ random projections; both metrics use the \emph{same} projection key per evaluation for variance-reduced paired estimates.}
    \label{tab: toy}
    \begin{tabular}{llcc}
        \toprule
        Dataset & Metric & MeanFlow & VaMF \\
        \midrule
        \texttt{checkerboard}    & SW$_1$ & $0.175 \pm 0.036$ & $\mathbf{0.134 \pm 0.029}$ \\
                                 & SW$_2$ & $0.234 \pm 0.042$ & $\mathbf{0.175 \pm 0.046}$ \\
        \midrule
        \texttt{eight\_gaussians}& SW$_1$ & $0.098 \pm 0.016$ & $\mathbf{0.085 \pm 0.010}$ \\
                                 & SW$_2$ & $0.151 \pm 0.022$ & $\mathbf{0.146 \pm 0.023}$ \\
        \midrule
        \texttt{two\_moons}      & SW$_1$ & $0.093 \pm 0.028$ & $\mathbf{0.072 \pm 0.019}$ \\
                                 & SW$_2$ & $0.177 \pm 0.068$ & $\mathbf{0.093 \pm 0.025}$ \\
        \midrule
        \texttt{swiss\_roll}     & SW$_1$ & $0.098 \pm 0.058$ & $\mathbf{0.045 \pm 0.003}$ \\
                                 & SW$_2$ & $0.177 \pm 0.151$ & $\mathbf{0.060 \pm 0.007}$ \\
        \midrule
        \texttt{two\_spirals}    & SW$_1$ & $\mathbf{0.034 \pm 0.001}$ & $0.037 \pm 0.001$ \\
                                 & SW$_2$ & $\mathbf{0.044 \pm 0.001}$ & $0.046 \pm 0.001$ \\
        \midrule
        \texttt{pinwheel}        & SW$_1$ & $0.094 \pm 0.011$ & $\mathbf{0.059 \pm 0.005}$ \\
                                 & SW$_2$ & $0.128 \pm 0.014$ & $\mathbf{0.076 \pm 0.005}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{High-Dimensional Scaling}
\label{subsec: exp-dgmm}

We sweep the data dimensionality on a dense Gaussian mixture (DGMM)---$64$ overlapping isotropic components on a unit sphere of radius $1.5$ in $\mathbb{R}^{d}$, $d\!\in\!\{2,4,8,16,32,64\}$. \Cref{tab: dgmm-scale} reports SW$_1$ at convergence (mean$\pm$standard error over three seeds). MeanFlow and VaMF are within one standard error at every $d$, with VaMF edging out at $d\!\in\!\{2,8,16\}$ and MeanFlow tying or marginally winning at $d\!\in\!\{32,64\}$. We attribute the absence of a widening gap to the fact that under our control-variate optimum, the noise floor reduces to $\Tr(\Sigma_{\vv'})$ (\Cref{prop: bias-var}) which is independent of $\mJ$. Since DGMM data is approximately isotropic Gaussian, $\Sigma_{\vv'}$ has structure that makes $\vb$ small---so the practical regime is already close to the unconstrained optimum, and the marginal benefit of variance reduction over the irreducible floor is limited.

\begin{table}[t]
    \centering
    \caption{DGMM scaling: SW$_1$ at convergence as the dimension $d$ grows (mean$\pm$standard error over three seeds, $200$k steps). Methods are within one standard error at every $d$.}
    \label{tab: dgmm-scale}
    \begin{tabular}{lcccccc}
        \toprule
        $d$ & $2$ & $4$ & $8$ & $16$ & $32$ & $64$ \\
        \midrule
        MeanFlow & $0.152{\scriptstyle\pm0.018}$ & $\mathbf{0.083}{\scriptstyle\pm0.009}$ & $0.062{\scriptstyle\pm0.003}$ & $0.044{\scriptstyle\pm0.002}$ & $\mathbf{0.034}{\scriptstyle\pm0.001}$ & $\mathbf{0.028}{\scriptstyle\pm0.003}$ \\
        VaMF     & $\mathbf{0.147}{\scriptstyle\pm0.012}$ & $0.083{\scriptstyle\pm0.009}$ & $\mathbf{0.061}{\scriptstyle\pm0.002}$ & $\mathbf{0.043}{\scriptstyle\pm0.002}$ & $0.036{\scriptstyle\pm0.002}$ & $0.028{\scriptstyle\pm0.001}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Convergence Stability}
\label{subsec: exp-stability}

\Cref{fig: convergence-stability} shows SW$_1$ vs training step on each toy dataset. Vanilla MeanFlow plateaus with persistent high-frequency oscillations, consistent with the gradient-variance amplification of~\Cref{thm: jacobi-variance}; VaMF converges smoothly to a lower steady-state SW$_1$, consistent with the variance reduction directly measured in~\Cref{fig: nr-vs-beta}. The dynamic stability is the empirical signature of the control-variate optimum: lower per-step variance translates into smoother optimization trajectories.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{assets/figures/toy_training_curves.pdf}
    \caption{Convergence stability across the 2-D toy benchmark: SW$_1$ vs training step (smoothed). Vanilla MeanFlow plateaus with persistent oscillations from the gradient variance of~\Cref{thm: jacobi-variance}; VaMF converges smoothly to a lower floor.}
    \label{fig: convergence-stability}
\end{figure}

\subsection{Discussion}
\label{subsec: exp-discussion}

The toy and DGMM results jointly validate the control-variate framework. (i) Direct gradient-variance measurement (\Cref{fig: nr-vs-beta}) confirms the predicted $\boldsymbol{g}^{\!\top}\!\mJ\Sigma_{\vv'}\mJ^{\!\top}\boldsymbol{g}$ scaling and shows $3\!\times\!\!\sim\!5\!\times$ reduction at the optimum on three of four datasets. (ii) Sample-quality improvements track the variance reduction: largest on high-curvature \texttt{swiss\_roll} ($-54\%$ SW$_{1}$) and \texttt{pinwheel} ($-37\%$); negligible on \texttt{two\_spirals} where MeanFlow already converges. (iii) On approximately isotropic DGMM data, the methods are statistically indistinguishable, consistent with the framework's prediction that the variance reduction shrinks once $\vb\!\to\!\bm{0}$ regardless of method. The same EMA-tangent mechanism transfers from $d{=}2$ toys to $d{=}4096$ image latents without architectural modification: a class-conditional DiT-B/4 / ImageNet-$256$ latent-space training shows VaMF maintains a small but consistent training-loss improvement over the matched MeanFlow baseline at a $\sim\!22\%$ wall-clock overhead per step (\Cref{appx: dit-results}).
